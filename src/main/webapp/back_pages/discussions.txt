1.对于google maps就是显示第一个结果的点坐标

2.由于geocoding的结果数量大于1，所以返回的结果数量就大于1；

每个结果都有一个点坐标和一个geojson，这个geojson可能是MultiPoint, MultiLineString, or MultiPolygon, or even GeometryCollection，

之前是对于linestring，显示所有结果；对于point和polygon则只显示第一个；（和linestring一样 我们并不知道后面的是不是也算是一部分）

现在是改成把所有返回的都organize成point linestring polygon

这种情况下，一个locadesc 可能就既有point也有别的 怎么判断；（之前是以第一个判断）

如果选择点，就返回第一个结果的点吗？

如果是multipoint 那是算作和point类型一样吗？

我的建议是不选择类型，如果用nominatim就显示所有的，如果用户想用点，就直接选择google maps；

3.request is too large

how to differentiate resolve and annoatate in the downloaded corpus?